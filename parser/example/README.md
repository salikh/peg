# Includes grammar example

This is an example of a very simple grammar intended to parse
the #include section of the C++ source file.

## How to run

    go run cmd/main.go --input_file=$PWD/input.txt

