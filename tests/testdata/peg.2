Grammar <- _ Rule*

Rule <- < Ident > _ '<-' _ RHS '\n' _
RHS <- Term *
Term <- '(' RHS ')' / '!' Term / '&' Term / '<' RHS '>' / CharClass / Ident

Ident <- < [a-zA-Z_][a-zA-Z0-9_]* >
CharClass <- '[' < ( ! ']' . ) * > ']'

_ <- ( [ \n\t\r] / '#' ( !'\n' .)* '\n' )*
